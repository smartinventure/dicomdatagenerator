namespace DicomDataGenerator.Models
{
    /// <summary>A generated patient name with the sex its first name matches ("M"/"F").</summary>
    public record GeneratedName(string Last, string First, string Sex);
}
